#include<stdio.h>
int main()
{
	int H=6, W=5, area , perimeter ;
	area=H*W;
	perimeter=2*(H+W);
	printf(" AREA = %d cm^2",area);
	printf( "\n" );
	printf(" PERIMETER = %d cm",perimeter);
	return 0;
}
